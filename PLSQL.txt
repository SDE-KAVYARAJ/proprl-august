PLSQL

--PLSQL

SELECT * FROM PRODUCT

--%TYPE

DECLARE
    V_WEIGHT PRODUCT.WEIGHT%TYPE;

BEGIN
--%TYPE
   SELECT WEIGHT INTO V_WEIGHT FROM PRODUCT WHERE PRODUCTID=11294;
   DBMS_OUTPUT.PUT_LINE('WEIGHT= ' || V_WEIGHT);

END



%ROWTYPE--STORE ONE ROW

DECLARE
    DATAVAR PRODUCT %ROWTYPE;

BEGIN
--%TYPE
   SELECT * INTO DATAVAR FROM PRODUCT WHERE PRODUCTID=11295;
   DBMS_OUTPUT.PUT_LINE(DATAVAR.PRODUCTID ||' '||DATAVAR.PRODUCTNAME||' '||DATAVAR.PRODUCTDESCRIPTION	||' '|| DATAVAR.PRICE	||' '|| DATAVAR.WEIGHT	||' '||DATAVAR.COMPANYCODE);

END


ASSIGNMENT
________

create table product3(productid int primary key not null,productname varchar(50) unique,productdescription varchar(50),price int,weight int,companycode int)

insert into product3 select * from product1

select * from product3

INSERT INTO PRODUCT1 VALUES(11299,'SANTHOOR','SOAP',50,20,112234)

DECLARE
    DATAVAR PRODUCT1 %ROWTYPE;

BEGIN
--%TYPE
   SELECT * INTO DATAVAR FROM PRODUCT1 WHERE PRODUCTID=11299;
   insert into product3 VALUES(DATAVAR.PRODUCTID,DATAVAR.PRODUCTNAME,DATAVAR.PRODUCTDESCRIPTION,DATAVAR.PRICE,DATAVAR.WEIGHT,DATAVAR.COMPANYCODE);
END


--BIND VARIABLE


--IF ELSE



